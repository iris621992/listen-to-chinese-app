// Styling pipeline note:
// Tailwind is not wired into PostCSS yet.
// The app currently relies on the manual utility layer in app/globals.css.
// Do not remove that utility layer until a separate Tailwind migration PR is planned and verified.
const config = {
  plugins: {},
};

export default config;
